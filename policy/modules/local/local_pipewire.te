policy_module(local_pipewire)

type pipewire_exec_t;
application_executable_file(pipewire_exec_t)

type pipewire_tmpfs_t;
userdom_user_tmpfs_file(pipewire_tmpfs_t)

type pipewire_runtime_t;
userdom_user_runtime_content(pipewire_runtime_t)

# Local PipeWire support is modeled like other per-user media/session services:
# a shared executable type with per-user domains instantiated through an
# interface template.
