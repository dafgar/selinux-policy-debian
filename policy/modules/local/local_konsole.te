policy_module(local_konsole)

gen_require(`
	type user_devpts_t;
	type system_dbusd_t;
	type sysadm_t;
	type setfiles_t;
')

attribute_role konsole_roles;

type konsole_t;
type konsole_exec_t;
userdom_user_application_domain(konsole_t, konsole_exec_t)
role konsole_roles types konsole_t;

# konsole is a pass-through: the shell and anything launched from it (git,
# sudo, editors, other GUI apps) need the same ability staff_t has to
# transition into the other per-app role domains, or opening a terminal
# would silently break launching anything else from inside it.
userdom_application_exec_domain(konsole_t, staff)

# Konsole itself allocates the pty for each terminal session. staff_t gets a
# user_devpts_t transition via userdom_base_user_template, but konsole_t (a
# plain userdom_user_application_domain) doesn't -- so without this, every
# pty Konsole opens defaults to plain devpts_t, and anything launched inside
# it (ssh_t, staff_git_t, sysadm elevation, ...) only has terminal grants
# scoped to user_devpts_t.
term_create_pty(konsole_t, user_devpts_t)

# sudo (still running as konsole_t at this point, before its own domain
# transition) talks to the system bus during PAM session setup -- the
# Hello handshake and its underlying stream_connect_pattern all need the
# usual system-bus-client grants.
dbus_system_bus_client(konsole_t)

# Opening a new terminal session registers it with logind
# (CreateSessionWithPIDFD) over the system bus.
systemd_dbus_chat_logind(konsole_t)

# dbus-daemon tracks connected clients via pidfd for peer-credential
# lookups; it needs to use that fd regardless of which bus API calls the
# client actually makes.
allow system_dbusd_t konsole_t:fd use;

# sudo/sesh (Debian's sudo session helper) elevate staff_r -> sysadm_r from
# a Konsole shell the same way they already can from a plain staff_t shell
# (sysadm_role_change(staff_r, staff_t) in roles/staff.te already covers the
# role change itself; these are the matching type-level grants konsole_t
# doesn't get for free the way userdom_base_user_template gives staff_t).
# Scoped narrowly to sysadm_t directly rather than the
# sysadm_application_exec_domain attribute, which would also pull in every
# unrelated sysadm-role app-domain transition in roles/sysadm.te.
allow konsole_t sysadm_t:process { transition noatsecure siginh rlimitinh signal };
allow sysadm_t konsole_t:fd use;
allow konsole_t sysadm_t:key create;
allow konsole_t self:process setkeycreate;
corecmd_exec_shell(konsole_t)

# setfiles run from that elevated sysadm_r shell (e.g. `sudo setfiles ...`)
# domain-transitions sysadm_t -> setfiles_t via seutil_run_setfiles, one hop
# past the sysadm_t grant above, so it still holds the inherited Konsole pty
# fd and needs its own use grant.
allow setfiles_t konsole_t:fd use;

# systemd-tty-ask-password-agent, run from that elevated sysadm_r shell, also
# inherits the Konsole pty fd (same one-hop situation as setfiles_t above).
systemd_use_passwd_agent_fds(konsole_t)
